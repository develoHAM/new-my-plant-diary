export interface loginData {
	email: string;
	password: string;
}

export interface postUserData {
	email: string;
	name: string;
	password: string;
}

export interface imgInfo {
	width: number;
	height: number;
}
